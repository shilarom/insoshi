# TODO Operate on the page object
if @location.success
  page << "geolocateAddress(#{@coord[0]},#{@coord[1]},'#{@location.full_address}', true);"
else
  page.alert("Sorry, we were unable to geocode that address")
  page << "map.clearOverlays();"
  page << '$("#event_lat").val(0);'
  page << '$("#event_lng").val(0);'
end
